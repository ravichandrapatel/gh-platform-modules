# CloudFront Distribution Module

CloudFront distribution with optional OAI/OAC, dynamic origins, cache behaviors, and custom error responses. Uses the tagging module.

<!-- BEGIN_TF_DOCS -->
## Requirements

| Name | Version |
|------|---------|
| <a name="requirement_terraform"></a> [terraform](#requirement\_terraform) | >= 1.10.0 |
| <a name="requirement_aws"></a> [aws](#requirement\_aws) | >= 5.0 |
| <a name="requirement_null"></a> [null](#requirement\_null) | >= 3.0 |

## Inputs

| Name | Description | Type | Default | Required |
|------|-------------|------|---------|:--------:|
| <a name="input_acm_certificate_arn"></a> [acm\_certificate\_arn](#input\_acm\_certificate\_arn) | ARN of ACM certificate for custom domain | `string` | `null` | no |
| <a name="input_alb_api_origin_arn"></a> [alb\_api\_origin\_arn](#input\_alb\_api\_origin\_arn) | Optional ALB ARN for the alb-api origin. When set (with alb\_api\_origin\_dns\_name), CloudFront reaches the ALB through a VPC origin so the ALB can be internal/private. When null, the alb-api origin is a public custom origin. | `string` | `null` | no |
| <a name="input_alb_api_origin_dns_name"></a> [alb\_api\_origin\_dns\_name](#input\_alb\_api\_origin\_dns\_name) | Optional ALB DNS name; API behaviors use alb\_api\_path\_patterns → alb-api origin. When null, no ALB origin or API behaviors. | `string` | `null` | no |
| <a name="input_alb_api_origin_protocol_policy"></a> [alb\_api\_origin\_protocol\_policy](#input\_alb\_api\_origin\_protocol\_policy) | Protocol CloudFront uses to reach the alb-api origin: 'http-only' (CloudFront→ALB over HTTP:80) or 'https-only' (over HTTPS:443). With 'https-only' the alb-api behaviors forward the viewer Host header so CloudFront validates the ALB certificate against that Host — the distribution MUST therefore use an alias covered by the ALB certificate (e.g. a *.rxapps360.com alias), otherwise CloudFront returns 502 on a cert mismatch. | `string` | `"http-only"` | no |
| <a name="input_alb_api_path_patterns"></a> [alb\_api\_path\_patterns](#input\_alb\_api\_path\_patterns) | Ordered path patterns sent to the ALB API origin when alb\_api\_origin\_dns\_name is set. Define in the caller (Terragrunt), not hardcoded per microservice in the module. | `list(string)` | <pre>[<br/>  "/rxcatalog-api*",<br/>  "/rxplan-api*",<br/>  "/rxasset-api*",<br/>  "/rxpo-api*",<br/>  "/core-api*"<br/>]</pre> | no |
| <a name="input_aliases"></a> [aliases](#input\_aliases) | Alternate domain names (CNAMEs) | `list(string)` | `[]` | no |
| <a name="input_api_subdomain_root_redirect"></a> [api\_subdomain\_root\_redirect](#input\_api\_subdomain\_root\_redirect) | When non-null, attach a CloudFront Function to the default cache behavior: for this Host header (case-insensitive) and URI exactly '/', return 302 to https://<host><root\_redirect\_path> (path your ALB serves, e.g. /health). | <pre>object({<br/>    host               = string<br/>    root_redirect_path = string<br/>  })</pre> | `null` | no |
| <a name="input_comment"></a> [comment](#input\_comment) | Comment for the distribution | `string` | `""` | no |
| <a name="input_create_oac"></a> [create\_oac](#input\_create\_oac) | Create Origin Access Control for S3 origins | `bool` | `false` | no |
| <a name="input_create_oai"></a> [create\_oai](#input\_create\_oai) | Create Origin Access Identity for S3 origins | `bool` | `false` | no |
| <a name="input_custom_error_responses"></a> [custom\_error\_responses](#input\_custom\_error\_responses) | Custom error responses (single set for the distribution). Prefer custom\_error\_responses\_per\_path for per-path config. | <pre>list(object({<br/>    error_code            = number<br/>    response_code         = number<br/>    response_page_path    = string<br/>    error_caching_min_ttl = number<br/>  }))</pre> | `[]` | no |
| <a name="input_custom_error_responses_per_path"></a> [custom\_error\_responses\_per\_path](#input\_custom\_error\_responses\_per\_path) | Custom error responses per path. Use path\_pattern = "default" for the root/default behavior; that entry's error\_responses are applied at distribution level. Other paths document intent (CloudFront does not support per-behavior error pages). | <pre>list(object({<br/>    path_pattern = string<br/>    error_responses = list(object({<br/>      error_code            = number<br/>      response_code         = number<br/>      response_page_path    = string<br/>      error_caching_min_ttl = number<br/>    }))<br/>  }))</pre> | `[]` | no |
| <a name="input_default_cache_behavior"></a> [default\_cache\_behavior](#input\_default\_cache\_behavior) | Default cache behavior | <pre>object({<br/>    target_origin_id       = string<br/>    viewer_protocol_policy = string<br/>    allowed_methods        = list(string)<br/>    cached_methods         = list(string)<br/>    compress               = bool<br/><br/>    cache_policy_id            = string<br/>    origin_request_policy_id   = string<br/>    response_headers_policy_id = string<br/>    realtime_log_config_arn    = string<br/><br/>    forwarded_values = object({<br/>      query_string = bool<br/>      headers      = list(string)<br/>      cookies = object({<br/>        forward           = string<br/>        whitelisted_names = list(string)<br/>      })<br/>    })<br/><br/>    min_ttl     = number<br/>    default_ttl = number<br/>    max_ttl     = number<br/><br/>    function_associations = list(object({<br/>      event_type   = string<br/>      function_arn = string<br/>    }))<br/><br/>    lambda_function_associations = list(object({<br/>      event_type   = string<br/>      lambda_arn   = string<br/>      include_body = bool<br/>    }))<br/>  })</pre> | n/a | yes |
| <a name="input_default_root_object"></a> [default\_root\_object](#input\_default\_root\_object) | Default root object | `string` | `"index.html"` | no |
| <a name="input_distribution_name"></a> [distribution\_name](#input\_distribution\_name) | Name of the CloudFront distribution | `string` | n/a | yes |
| <a name="input_enabled"></a> [enabled](#input\_enabled) | Enable CloudFront distribution | `bool` | `true` | no |
| <a name="input_geo_restriction_locations"></a> [geo\_restriction\_locations](#input\_geo\_restriction\_locations) | Country codes for geo restriction | `list(string)` | `[]` | no |
| <a name="input_geo_restriction_type"></a> [geo\_restriction\_type](#input\_geo\_restriction\_type) | Geo restriction type (none, whitelist, blacklist) | `string` | `"none"` | no |
| <a name="input_http_version"></a> [http\_version](#input\_http\_version) | Maximum HTTP version (http1.1, http2, http2and3, http3) | `string` | `"http2and3"` | no |
| <a name="input_invalidation_paths"></a> [invalidation\_paths](#input\_invalidation\_paths) | Optional list of path patterns to invalidate after the distribution is updated (triggers a local-exec create-invalidation; the AWS provider has no invalidation resource). Example: ["/rxcatalog*"]. | `list(string)` | `null` | no |
| <a name="input_is_ipv6_enabled"></a> [is\_ipv6\_enabled](#input\_is\_ipv6\_enabled) | Enable IPv6 | `bool` | `true` | no |
| <a name="input_logging"></a> [logging](#input\_logging) | Logging configuration | <pre>object({<br/>    enabled         = bool<br/>    include_cookies = bool<br/>    bucket          = string<br/>    prefix          = string<br/>  })</pre> | `null` | no |
| <a name="input_minimum_protocol_version"></a> [minimum\_protocol\_version](#input\_minimum\_protocol\_version) | Minimum TLS protocol version | `string` | `"TLSv1.2_2021"` | no |
| <a name="input_oac_name"></a> [oac\_name](#input\_oac\_name) | Name for Origin Access Control | `string` | `""` | no |
| <a name="input_oac_signing_behavior"></a> [oac\_signing\_behavior](#input\_oac\_signing\_behavior) | Signing behavior for OAC (always, never, no-override) | `string` | `"always"` | no |
| <a name="input_oac_signing_protocol"></a> [oac\_signing\_protocol](#input\_oac\_signing\_protocol) | Signing protocol for OAC (sigv4) | `string` | `"sigv4"` | no |
| <a name="input_oai_comment"></a> [oai\_comment](#input\_oai\_comment) | Comment for Origin Access Identity | `string` | `""` | no |
| <a name="input_ordered_cache_behaviors"></a> [ordered\_cache\_behaviors](#input\_ordered\_cache\_behaviors) | Ordered cache behaviors | <pre>list(object({<br/>    path_pattern           = string<br/>    target_origin_id       = string<br/>    viewer_protocol_policy = string<br/>    allowed_methods        = list(string)<br/>    cached_methods         = list(string)<br/>    compress               = bool<br/><br/>    cache_policy_id            = string<br/>    origin_request_policy_id   = string<br/>    response_headers_policy_id = string<br/><br/>    forwarded_values = object({<br/>      query_string = bool<br/>      headers      = list(string)<br/>      cookies = object({<br/>        forward           = string<br/>        whitelisted_names = list(string)<br/>      })<br/>    })<br/><br/>    min_ttl     = number<br/>    default_ttl = number<br/>    max_ttl     = number<br/><br/>    function_associations = list(object({<br/>      event_type   = string<br/>      function_arn = string<br/>    }))<br/><br/>    lambda_function_associations = list(object({<br/>      event_type   = string<br/>      lambda_arn   = string<br/>      include_body = bool<br/>    }))<br/>  }))</pre> | `[]` | no |
| <a name="input_origins"></a> [origins](#input\_origins) | List of origins for the distribution | <pre>list(object({<br/>    origin_id                = string<br/>    domain_name              = string<br/>    origin_path              = optional(string, "")<br/>    connection_attempts      = optional(number, 3)<br/>    connection_timeout       = optional(number, 10)<br/>    use_oac                  = optional(bool, false)<br/>    origin_access_control_id = optional(string, "")<br/><br/>    s3_origin_config = optional(object({<br/>      origin_access_identity   = string<br/>      origin_access_control_id = optional(string, "")<br/>    }), null)<br/><br/>    custom_origin_config = optional(object({<br/>      http_port                = number<br/>      https_port               = number<br/>      origin_protocol_policy   = string<br/>      origin_ssl_protocols     = list(string)<br/>      origin_keepalive_timeout = number<br/>      origin_read_timeout      = number<br/>    }), null)<br/><br/>    custom_headers = optional(list(object({<br/>      name  = string<br/>      value = string<br/>    })), null)<br/><br/>    origin_shield = optional(object({<br/>      enabled              = bool<br/>      origin_shield_region = string<br/>    }), null)<br/><br/>    vpc_origin_config = optional(object({<br/>      name                   = string<br/>      arn                    = string<br/>      http_port              = optional(number, 80)<br/>      https_port             = optional(number, 443)<br/>      origin_protocol_policy = optional(string, "https-only")<br/>      origin_ssl_protocols = optional(object({<br/>        items    = list(string)<br/>        quantity = number<br/>        }), {<br/>        items    = ["TLSv1.2"]<br/>        quantity = 1<br/>      })<br/>    }), null)<br/>  }))</pre> | n/a | yes |
| <a name="input_price_class"></a> [price\_class](#input\_price\_class) | Price class for CloudFront distribution. Set to null to omit (required for distributions on the Free pricing plan, which cannot have Price class set). | `string` | `null` | no |
| <a name="input_ssl_support_method"></a> [ssl\_support\_method](#input\_ssl\_support\_method) | SSL support method (sni-only or vip) | `string` | `"sni-only"` | no |
| <a name="input_tags"></a> [tags](#input\_tags) | Tags to apply to the CloudFront distribution | `map(string)` | `{}` | no |
| <a name="input_viewer_request_router_functions"></a> [viewer\_request\_router\_functions](#input\_viewer\_request\_router\_functions) | List of { path\_pattern, path\_prefix, optional function\_name } to create a CloudFront Function (viewer-request) that rewrites path\_prefix and route-like paths to path\_prefix/index.html. One function is created per entry and attached to the cache behavior with the matching path\_pattern. When function\_name is omitted, defaults to "{distribution\_name}-spa-router-{path\_prefix without slashes}". | <pre>list(object({<br/>    path_pattern  = string<br/>    path_prefix   = string<br/>    function_name = optional(string)<br/>  }))</pre> | `[]` | no |
| <a name="input_web_acl_id"></a> [web\_acl\_id](#input\_web\_acl\_id) | WAF Web ACL ID (optional). This module does not create or modify Web ACLs; pass only if the distribution already has one. Terraform lifecycle ignore\_changes prevents changing it. | `string` | `null` | no |

## Outputs

| Name | Description |
|------|-------------|
| <a name="output_aws_account_id"></a> [aws\_account\_id](#output\_aws\_account\_id) | AWS account ID that owns the distribution (for SourceArn-style policies) |
| <a name="output_distribution_arn"></a> [distribution\_arn](#output\_distribution\_arn) | CloudFront distribution ARN |
| <a name="output_distribution_domain_name"></a> [distribution\_domain\_name](#output\_distribution\_domain\_name) | CloudFront distribution domain |
| <a name="output_distribution_hosted_zone_id"></a> [distribution\_hosted\_zone\_id](#output\_distribution\_hosted\_zone\_id) | CloudFront hosted zone ID |
| <a name="output_distribution_id"></a> [distribution\_id](#output\_distribution\_id) | CloudFront distribution ID |
| <a name="output_oac_id"></a> [oac\_id](#output\_oac\_id) | OAC ID |
| <a name="output_oai_iam_arn"></a> [oai\_iam\_arn](#output\_oai\_iam\_arn) | OAI IAM ARN |
| <a name="output_oai_path"></a> [oai\_path](#output\_oai\_path) | OAI path |
| <a name="output_vpc_origin_ids"></a> [vpc\_origin\_ids](#output\_vpc\_origin\_ids) | Map of CloudFront VPC origin IDs by origin\_id |
<!-- END_TF_DOCS -->
